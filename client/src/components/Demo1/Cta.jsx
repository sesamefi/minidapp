function Cta({ token }) {
  return (
    <p>
      Donate some&nbsp;
      <span className="code">{token}</span>
      &nbsp;to&nbsp;the&nbsp;
      <span className="code">Donation</span>&nbsp;smart contract.
    </p>
  );
}

export default Cta;
